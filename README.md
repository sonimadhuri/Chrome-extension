# Chrome-extension
chrome extension for college teachers to save pin for validation
This chrome extensions allows users to save pin to the extension and next time the user visits the website, the extension auto populates the input box with the saved pin, and, saves the newly generated pin
